"""TravelPia backend — FastAPI service for AI, weather and image endpoints.

This package owns the compute/secret-bound slice of the TravelPia system
(Dev B in the architecture doc). It exposes a small, versioned HTTP contract
that the React Native app and other services build against.
"""

__version__ = "0.1.0"
